/**
 * @typedef {object} BitDiffusion
 * @property {string} id
 * @property {(payloadBits: string) => Promise<string>} preprocess
 * @property {(processedBits: string) => Promise<string>} postprocess
 */

export {};
